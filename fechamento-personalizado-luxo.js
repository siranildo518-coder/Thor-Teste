(()=>{let timer;const norm=s=>(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase();function decorate(root){if(!root||root.classList.contains('thor-fechamento-luxo'))return;root.classList.add('thor-fechamento-luxo');root.querySelectorAll('button').forEach(b=>{const q=norm(b.textContent).trim();if(q.includes('PALPITES'))b.classList.add('thor-lux-tab-active');if(['PARES','IMPARES','PRIMOS','FIBONACCI','MOLDURA','CENTRO','FIXAS','MULT. DE 3','MULT DE 3','SOMA'].some(v=>q===v))b.classList.add('thor-lux-filter');if(q.includes('LIMPAR FILTROS'))b.classList.add('thor-lux-clear');if(q.includes('GERAR JOGOS'))b.classList.add('thor-lux-gerar')})}function scan(){const nodes=[...document.querySelectorAll('body *')];const marker=nodes.find(el=>{const t=norm(el.textContent);return el.children.length<20&&t.includes('CONFIGURACOES AVANCADAS')});if(!marker)return;let root=marker;for(let i=0;i<10&&root.parentElement;i++){const p=root.parentElement,t=norm(p.textContent);root=p;if(t.includes('FILTROS PERSONALIZADOS')&&t.includes('GERAR JOGOS')&&(t.includes('NUMERO DE DEZENAS POR JOGO')||t.includes('QUANTIDADE DE JOGOS'))){decorate(root);return}}}function schedule(){clearTimeout(timer);timer=setTimeout(scan,60)}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',scan):scan();new MutationObserver(schedule).observe(document.documentElement,{childList:true,subtree:true,characterData:true})})();